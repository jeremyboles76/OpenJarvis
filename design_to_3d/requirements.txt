# Core — at least one vision provider must be installed
anthropic>=0.30          # Anthropic Claude vision (set ANTHROPIC_API_KEY)
openai>=1.30             # OpenAI GPT-4o vision   (set OPENAI_API_KEY)

# Optional — for GLB output (falls back to OBJ when absent)
trimesh>=4.0             # 3D mesh library for GLB/binary-glTF export
numpy>=1.24              # Required by trimesh

# Optional — for nicer CLI output
rich>=13
